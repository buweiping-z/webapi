# 点检系统性能优化实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 消除后端全表扫描/N+1查询/无分页/变更跟踪开销，修复手机端Snackbar丢失/数值验证bug/API调用风暴/资源泄漏。

**Architecture:** 后端：EF Core全局NoTracking + 3个新索引 + BuildFrequencyStats批量查询 + SaveDailyRecord原生SQL批量upsert + 分页 + DTO投影。手机端：防抖 + 资源释放 + 内存缓存 + 依赖注入。

**Tech Stack:** .NET 8.0 + EF Core 8.0 + Pomelo MySQL + Kotlin + Jetpack Compose + Retrofit + CameraX + ML Kit

---

### Task 1: 后端 — 全局 AsNoTracking + 索引配置

**Files:**
- Modify: `Data/AppDbContext.cs`

- [ ] **Step 1: 在 AppDbContext 构造函数中设置全局 NoTracking**

找到 `AppDbContext` 类的构造函数（约第8行），在 `{ }` 内添加一行：

```csharp
public AppDbContext(DbContextOptions<AppDbContext> options) : base(options)
{
    ChangeTracker.QueryTrackingBehavior = QueryTrackingBehavior.NoTracking;
}
```

- [ ] **Step 2: 在 OnModelCreating 中新增 3 个索引**

在 `OnModelCreating` 方法的末尾（`base.OnModelCreating(modelBuilder);` 之后），现有 InspectionResult 配置之后，追加：

```csharp
// ===== 性能优化索引 =====

// 唯一索引：支持 INSERT ... ON DUPLICATE KEY UPDATE 批量 upsert
modelBuilder.Entity<InspectionResult>(entity =>
{
    entity.HasIndex(r => new { r.RecordId, r.ItemName })
        .IsUnique()
        .HasDatabaseName("uq_results_record_item");
});

// 复合索引：月度时间范围查询
modelBuilder.Entity<InspectionRecord>(entity =>
{
    entity.HasIndex(r => new { r.DeviceModel, r.InspectionTime })
        .HasDatabaseName("idx_records_device_time");
});

// 复合索引：签名按月查询
modelBuilder.Entity<InspectionSignature>(entity =>
{
    entity.HasIndex(s => new { s.DeviceModel, s.Year, s.Month })
        .HasDatabaseName("idx_signatures_device_period");
});
```

注意：以上追加在现有 `OnModelCreating` 方法体内，不与已有配置冲突。`InspectionResult` 和 `InspectionRecord` 在 `OnModelCreating` 中已有配置块（分别在第53-61行、第25-39行），可以在各自现有配置块内追加 `.HasIndex(...)` 调用，而不是新建独立的 `modelBuilder.Entity<>()` 块。实际编辑时确认现有代码结构。

- [ ] **Step 3: 生成 EF Core Migration**

```bash
dotnet ef migrations add AddPerformanceIndexes
```

预期：`Migrations/` 目录下生成新的迁移文件，包含 `CreateIndex` 操作。

- [ ] **Step 4: 应用迁移**

```bash
dotnet ef database update
```

预期：成功执行，无报错。

- [ ] **Step 5: 验证索引已创建**

连接 MySQL，执行：
```sql
SHOW INDEX FROM inspection_results WHERE Key_name = 'uq_results_record_item';
SHOW INDEX FROM inspection_records WHERE Key_name = 'idx_records_device_time';
SHOW INDEX FROM inspection_signatures WHERE Key_name = 'idx_signatures_device_period';
```

预期：各返回一行索引信息。

- [ ] **Step 6: 为需要更新的查询显式加 AsTracking()**

全局 NoTracking 后，以下位置需要显式 `.AsTracking()`。在 `Controllers/InspectionController.cs` 中：

**位置 1** — `SubmitFullInspection` 方法（约第86行），`FirstOrDefaultAsync` 查已有 record：
```csharp
var existingRecord = await _context.InspectionRecords
    .AsTracking()   // ← 新增
    .FirstOrDefaultAsync(r => r.DeviceModel == request.DeviceModel
        && r.Frequency == frequency
        && r.PeriodKey == periodKey);
```

**位置 2** — `SaveDailyRecord` 方法（约第210行），`FirstOrDefaultAsync` 查已有 record：
```csharp
var existingRecord = await _context.InspectionRecords
    .AsTracking()   // ← 新增
    .FirstOrDefaultAsync(r => r.DeviceModel == request.DeviceModel
        && r.Frequency == frequency
        && r.PeriodKey == periodKey);
```

**位置 3** — `SaveDailyRecord` 方法（约第218行），`FirstOrDefaultAsync` 查已有 result：
```csharp
var existingResult = await _context.InspectionResults
    .AsTracking()   // ← 新增
    .FirstOrDefaultAsync(res => res.RecordId == existingRecord.Id && res.ItemName == item.ItemName);
```

**位置 4** — `SaveSignatures` 方法（约第314行），`FirstOrDefaultAsync` 查已有 signature：
```csharp
var existing = await _context.InspectionSignatures
    .AsTracking()   // ← 新增
    .FirstOrDefaultAsync(s => s.DeviceModel == request.DeviceModel
        && s.Year == request.Year
        && s.Month == request.Month);
```

**位置 5** — `AutoSign` 方法（约第389行），`FirstOrDefaultAsync` 查已有 signature：
```csharp
var existing = await _context.InspectionSignatures
    .AsTracking()   // ← 新增
    .FirstOrDefaultAsync(s => s.DeviceModel == request.DeviceModel
        && s.Year == request.Year
        && s.Month == request.Month);
```

**位置 6** — `DeleteOperator` 方法（约第481行），`FirstOrDefaultAsync` 查 operator：
```csharp
var operator_ = await _context.QualifiedInspectors
    .AsTracking()   // ← 新增
    .FirstOrDefaultAsync(o => o.EmployeeId == employeeId);
```

- [ ] **Step 7: 构建并运行确认无编译错误**

```bash
dotnet build
```

预期：Build succeeded.

- [ ] **Step 8: 启动并快速冒烟测试**

```bash
dotnet run
```

打开 `http://localhost:5039/swagger`，测试：
- `GET /api/Inspection/templates/{deviceModel}` — 应正常返回模板
- `GET /api/Inspection/devices` — 应正常返回设备列表

- [ ] **Step 9: Commit**

```bash
git add Data/AppDbContext.cs Controllers/InspectionController.cs Migrations/
git commit -m "perf: add global AsNoTracking + 3 performance indexes

- Set ChangeTracker.QueryTrackingBehavior = NoTracking globally
- Add uq_results_record_item UNIQUE index for bulk upsert support
- Add idx_records_device_time for monthly time-range queries
- Add idx_signatures_device_period for signature lookups
- Add explicit AsTracking() to 6 write/delete query sites

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: 后端 — BuildFrequencyStats 批量化

**Files:**
- Modify: `Controllers/InspectionController.cs:710-836`

- [ ] **Step 1: 替换 BuildFrequencyStats 方法**

将当前 `BuildFrequencyStats` 方法（约第710-836行）整个替换为以下批量查询版本。关键改动：foreach device 内的 3 次独立查询 → 3 次批量查询 + 内存分组。

```csharp
/// <summary>
/// 统计指定频率在指定月份的设备点检情况（批量查询优化版）
/// 日点检只查当天，周点检查本周，月点检查整月
/// </summary>
private async Task<object> BuildFrequencyStats(string frequency, int year, int month, DateTime startDate, DateTime endDate)
{
    // 拥有该频率模板的设备列表
    var devices = await _context.InspectionTemplates
        .Where(t => t.Frequency == frequency)
        .Select(t => t.DeviceModel)
        .Distinct()
        .ToListAsync();

    var totalDevices = devices.Count;
    if (totalDevices == 0)
    {
        return new
        {
            totalDevices = 0,
            inspectedDevices = 0,
            uninspectedDevices = 0,
            uninspectedDeviceModels = new List<string>(),
            abnormalDevices = 0,
            abnormalDeviceModels = new List<string>()
        };
    }

    // 按周期确定实际查询的时间范围
    var now = DateTime.Now;
    DateTime periodStart, periodEnd;
    if (frequency == "日")
    {
        var today = now.Date;
        if (today >= startDate && today <= endDate)
        {
            periodStart = today;
            periodEnd = today.AddDays(1).AddTicks(-1);
        }
        else
        {
            periodStart = endDate.Date;
            periodEnd = endDate.Date.AddDays(1).AddTicks(-1);
        }
    }
    else if (frequency == "周")
    {
        int dayOfWeek = (int)now.DayOfWeek;
        int diff = dayOfWeek == 0 ? 6 : dayOfWeek - 1;
        var monday = now.Date.AddDays(-diff);
        if (monday < startDate) monday = startDate;
        var sunday = monday.AddDays(7).AddTicks(-1);
        if (sunday > endDate) sunday = endDate;
        periodStart = monday;
        periodEnd = sunday;
    }
    else
    {
        periodStart = startDate;
        periodEnd = endDate;
    }

    // ===== 批量查询 1：所有设备的必须点检项目（一次 SQL） =====
    var allMandatoryItems = await _context.InspectionTemplates
        .Where(t => devices.Contains(t.DeviceModel)
                    && t.Frequency == frequency
                    && t.IsMandatory)
        .Select(t => new { t.DeviceModel, t.ItemName })
        .ToListAsync();

    // 按设备分组
    var mandatoryByDevice = allMandatoryItems
        .GroupBy(t => t.DeviceModel)
        .ToDictionary(g => g.Key, g => g.Select(x => x.ItemName).ToHashSet());

    // ===== 批量查询 2：所有设备在周期内的记录（一次 SQL） =====
    var allRecords = await _context.InspectionRecords
        .Where(r => devices.Contains(r.DeviceModel)
                    && r.Frequency == frequency
                    && r.InspectionTime >= periodStart
                    && r.InspectionTime <= periodEnd)
        .ToListAsync();

    // 按设备分组
    var recordsByDevice = allRecords
        .GroupBy(r => r.DeviceModel)
        .ToDictionary(g => g.Key, g => g.ToList());

    // ===== 批量查询 3：所有记录的结果明细（一次 SQL） =====
    var allRecordIds = allRecords.Select(r => r.Id).ToList();
    List<InspectionResult> allResults;
    if (allRecordIds.Count > 0)
    {
        // 分批处理 IN 子句（防止 ID 过多导致 SQL 过大）
        allResults = new List<InspectionResult>();
        const int batchSize = 500;
        for (int i = 0; i < allRecordIds.Count; i += batchSize)
        {
            var batch = allRecordIds.Skip(i).Take(batchSize).ToList();
            var batchResults = await _context.InspectionResults
                .Where(res => batch.Contains(res.RecordId))
                .ToListAsync();
            allResults.AddRange(batchResults);
        }
    }
    else
    {
        allResults = new List<InspectionResult>();
    }

    // 按 recordId 分组
    var resultsByRecordId = allResults
        .GroupBy(res => res.RecordId)
        .ToDictionary(g => g.Key, g => g.ToList());

    // ===== 内存中按设备统计 =====
    var inspectedCount = 0;
    var abnormalCount = 0;
    var uninspectedDevices = new List<string>();
    var abnormalDeviceModels = new List<string>();

    foreach (var device in devices)
    {
        // 获取该设备必须点检的项目名集合
        if (!mandatoryByDevice.TryGetValue(device, out var mandatorySet) || mandatorySet.Count == 0)
            continue;

        // 获取该设备在周期内的记录
        if (!recordsByDevice.TryGetValue(device, out var deviceRecords) || deviceRecords.Count == 0)
        {
            uninspectedDevices.Add(device);
            continue;
        }

        // 收集本周期所有记录的 result item_name
        var deviceResultItemNames = new HashSet<string>();
        var anyAbnormal = false;
        foreach (var record in deviceRecords)
        {
            if (resultsByRecordId.TryGetValue(record.Id, out var results))
            {
                foreach (var r in results)
                {
                    deviceResultItemNames.Add(r.ItemName);
                    if (!r.IsNormal) anyAbnormal = true;
                }
            }
        }

        // 检查是否所有必须点检项目都有结果
        var allMandatoryInspected = mandatorySet.All(item => deviceResultItemNames.Contains(item));

        if (!allMandatoryInspected)
        {
            uninspectedDevices.Add(device);
        }
        else if (anyAbnormal)
        {
            abnormalCount++;
            abnormalDeviceModels.Add(device);
        }
        else
        {
            inspectedCount++;
        }
    }

    var uninspectedCount = uninspectedDevices.Count;

    return new
    {
        totalDevices,
        inspectedDevices = inspectedCount,
        uninspectedDevices = uninspectedCount,
        uninspectedDeviceModels = uninspectedDevices,
        abnormalDevices = abnormalCount,
        abnormalDeviceModels
    };
}
```

- [ ] **Step 2: 构建确认无编译错误**

```bash
dotnet build
```

预期：Build succeeded.

- [ ] **Step 3: 启动并通过 Swagger 验证 GetFrequencySummary**

```bash
dotnet run
```

打开 `http://localhost:5039/swagger`，调用 `GET /api/Inspection/frequency-summary?year=2026&month=6`。

预期：返回 daily/weekly/monthly 统计，结果与优化前一致。

- [ ] **Step 4: Commit**

```bash
git add Controllers/InspectionController.cs
git commit -m "perf: refactor BuildFrequencyStats to batch queries (3N→3 SQLs)

Eliminate N+1 pattern: previously 3 queries per device in a foreach loop,
now 3 batch queries for all devices + in-memory grouping.
Added IN clause batching (500 IDs per batch) for safety.

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: 后端 — SaveDailyRecord 批量 Upsert

**Files:**
- Modify: `Controllers/InspectionController.cs:188-274`

**前置条件**: Task 1 中的 `uq_results_record_item` 唯一索引已创建。

- [ ] **Step 1: 替换 SaveDailyRecord 方法**

将当前 `SaveDailyRecord` 方法（约第188-274行）整个替换为以下事务+批量upsert版本：

```csharp
// 4. 保存点检记录（事务 + 批量 upsert 优化版）
[HttpPost("records/save")]
public async Task<IActionResult> SaveDailyRecord([FromBody] SaveDailyRecordRequest request)
{
    try
    {
        var inspectionDate = request.InspectionMonth;
        var employeeId = string.IsNullOrEmpty(request.EmployeeId) ? "web" : request.EmployeeId;
        var frequency = "日";

        // 收集所有周期键
        var periodKeys = request.Results
            .Select(item => $"{inspectionDate.Year:0000}-{inspectionDate.Month:00}-{item.Day:00}")
            .Distinct()
            .ToList();

        using var transaction = await _context.Database.BeginTransactionAsync();
        try
        {
            // ===== 批量查询 1：查出所有已有的 records =====
            var existingRecords = await _context.InspectionRecords
                .AsTracking()
                .Where(r => r.DeviceModel == request.DeviceModel
                            && r.Frequency == frequency
                            && periodKeys.Contains(r.PeriodKey))
                .ToListAsync();

            var existingRecordMap = existingRecords
                .ToDictionary(r => r.PeriodKey);

            // 补齐缺失的 records
            foreach (var periodKey in periodKeys)
            {
                if (!existingRecordMap.ContainsKey(periodKey))
                {
                    // 从 results 中找到该 periodKey 对应的 day
                    var dayItem = request.Results.FirstOrDefault(r =>
                        $"{inspectionDate.Year:0000}-{inspectionDate.Month:00}-{r.Day:00}" == periodKey);
                    var day = dayItem?.Day ?? 1;

                    var newRecord = new InspectionRecord
                    {
                        EmployeeId = employeeId,
                        DeviceModel = request.DeviceModel,
                        DeviceName = request.DeviceModel,
                        InspectionTime = new DateTime(inspectionDate.Year, inspectionDate.Month, day),
                        Status = "submitted",
                        ResultsJson = "",
                        Frequency = frequency,
                        PeriodKey = periodKey
                    };
                    _context.InspectionRecords.Add(newRecord);
                    await _context.SaveChangesAsync();
                    existingRecordMap[periodKey] = newRecord;
                }
            }

            // ===== 批量 upsert results =====
            var upsertSql = @"
                INSERT INTO inspection_results (record_id, item_name, result_value, is_normal, remark)
                VALUES ({0}, {1}, {2}, {3}, {4})
                ON DUPLICATE KEY UPDATE
                    result_value = VALUES(result_value),
                    is_normal = VALUES(is_normal),
                    remark = VALUES(remark)";

            foreach (var item in request.Results)
            {
                // 转换前端传来的值：○ → 正常，× → 异常
                string saveValue = item.ResultValue;
                if (saveValue == "○")
                    saveValue = "正常";
                else if (saveValue == "×")
                    saveValue = "异常";

                var periodKey = $"{inspectionDate.Year:0000}-{inspectionDate.Month:00}-{item.Day:00}";
                if (!existingRecordMap.TryGetValue(periodKey, out var record))
                    continue;

                await _context.Database.ExecuteSqlRawAsync(upsertSql,
                    record.Id, item.ItemName, saveValue, saveValue == "正常", item.Remark);
            }

            await transaction.CommitAsync();
            return Ok(new { success = true, message = "保存成功" });
        }
        catch
        {
            await transaction.RollbackAsync();
            throw;
        }
    }
    catch (Exception ex)
    {
        return StatusCode(500, new { success = false, message = ex.Message });
    }
}
```

- [ ] **Step 2: 构建确认无编译错误**

```bash
dotnet build
```

预期：Build succeeded.

- [ ] **Step 3: 通过 Swagger 测试 SaveDailyRecord**

```bash
dotnet run
```

打开 `http://localhost:5039/swagger`，调用 `POST /api/Inspection/records/save`，传入测试数据：

```json
{
  "employeeId": "web",
  "deviceModel": "<选择一个存在的设备型号>",
  "inspectionMonth": "2026-06-01",
  "results": [
    { "day": 15, "itemName": "<一个存在的模板项>", "resultValue": "正常", "remark": "测试" }
  ]
}
```

预期：返回 `{"success": true, "message": "保存成功"}`。

再查一次 `GET /api/Inspection/records/monthly` 确认数据已写入。

- [ ] **Step 4: Commit**

```bash
git add Controllers/InspectionController.cs
git commit -m "perf: refactor SaveDailyRecord to transactional batch upsert

Replace per-item query+insert/update loop (2N SQLs) with:
1. Batch query existing records by periodKeys
2. INSERT ... ON DUPLICATE KEY UPDATE for all results
3. Wrapped in explicit transaction for atomicity

Requires uq_results_record_item UNIQUE index from Task 1.

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: 后端 — GetDailyOperators 窗口函数优化

**Files:**
- Modify: `Controllers/InspectionController.cs:511-552`

- [ ] **Step 1: 替换 GetDailyOperators 方法**

将当前方法（约第511-552行）替换为使用 `ROW_NUMBER()` 窗口函数的版本：

```csharp
// 18. 获取指定设备+年月的每日点检者姓（窗口函数优化版）
[HttpGet("operators/daily")]
public async Task<IActionResult> GetDailyOperators(string deviceModel, int year, int month)
{
    var startDate = new DateTime(year, month, 1);
    var endDate = startDate.AddMonths(1).AddDays(-1);

    // 用 ROW_NUMBER() 窗口函数在数据库端按日分组取最新记录
    var sql = @"
        SELECT day, employee_id FROM (
            SELECT 
                DAY(inspection_time) as day,
                employee_id,
                ROW_NUMBER() OVER (PARTITION BY DAY(inspection_time) ORDER BY inspection_time DESC) as rn
            FROM inspection_records
            WHERE device_model = {0} 
                AND inspection_time >= {1} 
                AND inspection_time <= {2}
        ) t WHERE rn = 1";

    var dailyRecords = await _context.Database
        .SqlQueryRaw<DailyOperatorRaw>(sql, deviceModel, startDate, endDate)
        .ToListAsync();

    // JOIN qualified_inspectors 获取姓
    var employeeIds = dailyRecords.Select(r => r.EmployeeId).Distinct().ToList();
    Dictionary<string, string> inspectors;
    if (employeeIds.Count > 0)
    {
        inspectors = await _context.QualifiedInspectors
            .Where(o => employeeIds.Contains(o.EmployeeId))
            .ToDictionaryAsync(o => o.EmployeeId, o => o.LastName);
    }
    else
    {
        inspectors = new Dictionary<string, string>();
    }

    var result = new Dictionary<string, string>();
    foreach (var dr in dailyRecords)
    {
        result[dr.Day.ToString()] = inspectors.TryGetValue(dr.EmployeeId, out var name) ? name : "";
    }

    return Ok(new
    {
        deviceModel,
        year,
        month,
        inspectors = result
    });
}
```

需要在 Controller 文件底部的请求模型区域添加一个轻量 DTO 用于原始 SQL 映射：

```csharp
// 用于 SqlQueryRaw 的临时映射类
public class DailyOperatorRaw
{
    public int Day { get; set; }
    public string EmployeeId { get; set; } = string.Empty;
}
```

把这个类放在 Controller 文件末尾（其他 DTO 附近，约第1162行之前）。

- [ ] **Step 2: 确认 MySQL 版本 ≥ 8.0**

```bash
# 连接 MySQL 执行：
SELECT VERSION();
```

如果返回 5.7.x，则改用 LINQ GroupBy 方案（见 Step 2b）。MySQL 8.0+ 直接继续。

- [ ] **Step 2b（仅在 MySQL 5.7 时执行）: LINQ GroupBy 替代方案**

如果不支持窗口函数，使用 LINQ 替代：

```csharp
// 查询该设备当月所有记录
var records = await _context.InspectionRecords
    .Where(r => r.DeviceModel == deviceModel
        && r.InspectionTime >= startDate
        && r.InspectionTime <= endDate)
    .GroupBy(r => r.InspectionTime.Day)
    .Select(g => new DailyOperatorRaw
    {
        Day = g.Key,
        EmployeeId = g.OrderByDescending(r => r.InspectionTime)
                       .Select(r => r.EmployeeId)
                       .FirstOrDefault() ?? ""
    })
    .ToListAsync();

// 后续 JOIN qualified_inspectors 逻辑不变（同 Step 1）
```

- [ ] **Step 3: 构建并验证**

```bash
dotnet build
dotnet run
```

通过 Swagger 调用 `GET /api/Inspection/operators/daily?deviceModel=<model>&year=2026&month=6`。
预期：返回按日分组的点检者姓，结果与优化前一致。

- [ ] **Step 4: Commit**

```bash
git add Controllers/InspectionController.cs
git commit -m "perf: optimize GetDailyOperators with ROW_NUMBER window function

Replace in-memory GroupBy (loading all month records) with SQL
ROW_NUMBER() OVER (PARTITION BY day ORDER BY inspection_time DESC).
Falls back to LINQ GroupBy for MySQL 5.7 compatibility.

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: 后端 — PagedResponse<T> 通用分页

**Files:**
- Create: `Models/PagedResponse.cs`
- Modify: `Controllers/InspectionController.cs:146-186` (GetMonthlyRecords)
- Modify: `Controllers/InspectionController.cs:351-360` (GetUserList)
- Modify: `Controllers/InspectionController.cs:432-440` (GetOperatorList)

- [ ] **Step 1: 创建 PagedResponse<T> 通用 DTO**

新建文件 `Models/PagedResponse.cs`：

```csharp
namespace webapi.Models;

/// <summary>
/// 通用分页响应 DTO
/// </summary>
public class PagedResponse<T>
{
    public List<T> Items { get; set; } = new();
    public int Page { get; set; }
    public int PageSize { get; set; }
    public int TotalCount { get; set; }
    public int TotalPages => PageSize > 0 ? (int)Math.Ceiling(TotalCount / (double)PageSize) : 0;
}
```

- [ ] **Step 2: 改造 GetMonthlyRecords 加分页**

替换当前 `GetMonthlyRecords` 方法（约第146-186行）：

```csharp
[HttpGet("records/monthly")]
public async Task<IActionResult> GetMonthlyRecords(
    string deviceModel, int year, int month,
    int page = 1, int pageSize = 100)
{
    try
    {
        var startDate = new DateTime(year, month, 1);
        var endDate = startDate.AddMonths(1).AddDays(-1);

        var query = 
            from r in _context.InspectionRecords
            join res in _context.InspectionResults on r.Id equals res.RecordId
            where r.DeviceModel == deviceModel
                  && r.InspectionTime >= startDate
                  && r.InspectionTime <= endDate
            select new MonthlyRecordDto
            {
                ItemName = res.ItemName,
                InspectionDay = r.InspectionTime.Day,
                ResultValue = res.ResultValue,
                Remark = res.Remark,
                IsNormal = res.IsNormal
            };

        var totalCount = await query.CountAsync();

        var records = await query
            .OrderByDescending(r => r.InspectionDay)
            .ThenByDescending(r => r.ItemName) // 稳定排序，防止分页重复/遗漏
            .Skip((page - 1) * pageSize)
            .Take(pageSize)
            .ToListAsync();

        // 转换结果值：正常 → ○，异常 → ×
        foreach (var record in records)
        {
            if (record.ResultValue == "正常")
                record.ResultValue = "○";
            else if (record.ResultValue == "异常")
                record.ResultValue = "×";
        }

        return Ok(new PagedResponse<MonthlyRecordDto>
        {
            Items = records,
            Page = page,
            PageSize = pageSize,
            TotalCount = totalCount
        });
    }
    catch (Exception ex)
    {
        return StatusCode(500, new { error = ex.Message });
    }
}
```

- [ ] **Step 3: 改造 GetUserList 加分页**

替换当前 `GetUserList` 方法（约第351-360行）：

```csharp
// 7. 获取用户列表（用于下拉菜单），支持分页
[HttpGet("users/list")]
public async Task<IActionResult> GetUserList(int page = 1, int pageSize = 100)
{
    var query = _context.InspectionUsers
        .Select(u => new { u.Username, u.FullName, u.Role });

    var totalCount = await query.CountAsync();

    var users = await query
        .OrderBy(u => u.Username)
        .Skip((page - 1) * pageSize)
        .Take(pageSize)
        .ToListAsync();

    return Ok(new PagedResponse<object>
    {
        Items = users.Cast<object>().ToList(),
        Page = page,
        PageSize = pageSize,
        TotalCount = totalCount
    });
}
```

- [ ] **Step 4: 改造 GetOperatorList 加分页**

替换当前 `GetOperatorList` 方法（约第432-440行）：

```csharp
// 14. 获取点检资格人员列表，支持分页
[HttpGet("operators/list")]
public async Task<IActionResult> GetOperatorList(int page = 1, int pageSize = 100)
{
    var query = _context.QualifiedInspectors
        .Select(o => new { o.EmployeeId, o.LastName });

    var totalCount = await query.CountAsync();

    var operators = await query
        .OrderBy(o => o.EmployeeId)
        .Skip((page - 1) * pageSize)
        .Take(pageSize)
        .ToListAsync();

    return Ok(new PagedResponse<object>
    {
        Items = operators.Cast<object>().ToList(),
        Page = page,
        PageSize = pageSize,
        TotalCount = totalCount
    });
}
```

- [ ] **Step 5: 构建并验证分页**

```bash
dotnet build
dotnet run
```

通过 Swagger 测试：

1. `GET /api/Inspection/records/monthly?deviceModel=<model>&year=2026&month=6&page=1&pageSize=2`
   - 确认返回 `items` 只有 2 条，`totalCount` 显示总条数，`totalPages` 计算正确

2. `GET /api/Inspection/users/list?page=1&pageSize=5`
   - 确认分页正确

3. `GET /api/Inspection/operators/list?page=1&pageSize=5`
   - 确认分页正确

- [ ] **Step 6: Commit**

```bash
git add Models/PagedResponse.cs Controllers/InspectionController.cs
git commit -m "feat: add pagination support with PagedResponse<T>

- Create generic PagedResponse<T> DTO
- Add optional page/pageSize params to GetMonthlyRecords, GetUserList, GetOperatorList
- Default page=1, pageSize=100 for backward compatibility
- Add stable ORDER BY clauses to prevent duplicate/missing rows across pages

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 6: 后端 — DTO 投影优化

**Files:**
- Modify: `Controllers/InspectionController.cs` (多个 GET 端点)
- Create: `Models/DTOs/` 目录下可按需创建 DTO 文件（或直接内联在 Controller 末尾）

- [ ] **Step 1: GetDevices 加 Select 投影**

修改 `GetDevices` 方法（约第132-144行），用 `.Select()` 投影替代拉取全列：

```csharp
// 获取所有设备型号
[HttpGet("devices")]
public async Task<IActionResult> GetDevices()
{
    var devices = await _context.InspectionTemplates
        .Select(t => new {
            deviceModel = t.DeviceModel,
            deviceName = t.DeviceModel
        })
        .Distinct()
        .ToListAsync();

    return Ok(devices);
}
```

- [ ] **Step 2: GetUserList 用 Select 投影（已有，确认无变更）**

确认 `GetUserList` 已在 Task 5 Step 3 中使用了 `.Select()` 投影。无误。

- [ ] **Step 3: GetOperatorList 用 Select 投影（已有，确认无变更）**

确认 `GetOperatorList` 已在 Task 5 Step 4 中使用了 `.Select()` 投影。无误。

- [ ] **Step 4: GetSignSummary 使用 Select 投影优化**

修改 `GetSignSummary` 方法中 `InspectionTemplates` 查询（约第561-564行），用 `.Select()` 替代拉取全列：

```csharp
var totalDevices = await _context.InspectionTemplates
    .Select(t => t.DeviceModel)
    .Distinct()
    .CountAsync();
```

（当前已是 Select + Count，确认无变更即可。）

- [ ] **Step 5: CheckAllSignatures 确保 Select 投影**

确认 `CheckAllSignatures` 方法（约第598-643行）中：
```csharp
var allDevices = await _context.InspectionTemplates
    .Select(t => t.DeviceModel)
    .Distinct()
    .ToListAsync();
```
（当前已是 Select + Distinct，确认无变更即可。）

- [ ] **Step 6: GetMonthlySummary 优化**

确认 `GetMonthlySummary` 方法（约第839-902行）已有充分的 Select 投影。

- [ ] **Step 7: 构建并冒烟测试**

```bash
dotnet build
dotnet run
```

通过 Swagger 快速验证所有 GET 端点返回数据正确。

- [ ] **Step 8: Commit**

```bash
git add Controllers/InspectionController.cs
git commit -m "perf: ensure all GET endpoints use Select projection

Verify/explicitly add .Select() projection to all read-only endpoints
to avoid SELECT * and reduce network transfer. Combined with global
AsNoTracking, this eliminates unnecessary column reads.

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 7: 手机端 — Bug 修复 (Snackbar + Double.MIN_VALUE)

**Files:**
- Modify: `machine_check/app/src/main/java/com/machine_check/inspection/ui/inspection/InspectionScreen.kt:38`
- Modify: `machine_check/app/src/main/java/com/machine_check/inspection/ui/inspection/InspectionViewModel.kt:141-145`

- [ ] **Step 1: SnackbarHostState 加 remember**

在 `InspectionScreen.kt` 第38行，将：
```kotlin
val snackbarHostState = androidx.compose.material3.SnackbarHostState()
```
改为：
```kotlin
val snackbarHostState = remember { androidx.compose.material3.SnackbarHostState() }
```

- [ ] **Step 2: Double.MIN_VALUE → null 检查**

在 `InspectionViewModel.kt` 第141-145行，将：
```kotlin
"numeric" -> {
    val numValue = item.numericValue.toDoubleOrNull()
    numValue != null &&
            numValue >= (template.normalMin ?: Double.MIN_VALUE) &&
            numValue <= (template.normalMax ?: Double.MAX_VALUE)
}
```
改为：
```kotlin
"numeric" -> {
    val numValue = item.numericValue.toDoubleOrNull()
    numValue != null &&
            (template.normalMin == null || numValue >= template.normalMin) &&
            (template.normalMax == null || numValue <= template.normalMax)
}
```

- [ ] **Step 3: 构建手机端 APK**

```bash
cd machine_check
./gradlew :app:assembleDebug
```

预期：BUILD SUCCESSFUL。

- [ ] **Step 4: Commit**

```bash
cd machine_check
git add app/src/main/java/com/machine_check/inspection/ui/inspection/
git commit -m "fix: SnackbarHostState remember + Double.MIN_VALUE validation bug

- Wrap SnackbarHostState in remember{} to prevent lost messages on recomposition
- Fix Double.MIN_VALUE (smallest positive) used as 'no minimum' — negative
  values were incorrectly rejected when normalMin was null. Use null check instead.

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 8: 手机端 — ScanViewModel 防抖 + Job Cancel

**Files:**
- Modify: `machine_check/app/src/main/java/com/machine_check/inspection/ui/scan/ScanViewModel.kt`

- [ ] **Step 1: 添加 Job 引用和 debounce 常量和 import**

在 `ScanViewModel.kt` 文件顶部 import 区域添加：
```kotlin
import kotlinx.coroutines.Job
import kotlinx.coroutines.delay
```

在 `ScanViewModel` 类内部（约第48行 `private val repository` 之后）添加：
```kotlin
private var freqCheckJob: Job? = null
```

- [ ] **Step 2: 重写 checkFrequenciesAvailable 方法**

替换当前 `checkFrequenciesAvailable` 方法（第215-250行）为带防抖和取消的版本：

```kotlin
/** 查询设备各频率的可用状态（300ms 防抖 + 自动取消前一个请求） */
private fun checkFrequenciesAvailable(deviceModel: String) {
    if (deviceModel.isBlank()) return
    
    // 取消前一个未完成的请求
    freqCheckJob?.cancel()
    
    freqCheckJob = viewModelScope.launch {
        // 300ms 防抖：等待用户停止输入
        delay(300)
        
        _uiState.update { it.copy(isCheckingFrequencies = true) }
        repository.getFrequenciesAvailable(deviceModel).fold(
            onSuccess = { result ->
                _uiState.update {
                    var state = it.copy(
                        isCheckingFrequencies = false,
                        frequenciesAvailable = result
                    )
                    // 如果当前选中的频率已不可用，自动切到第一个可用的
                    val sel = it.selectedFrequency
                    if (sel == "日" && !result.daily.available) {
                        val fallback = when {
                            result.weekly.available -> "周"
                            result.monthly.available -> "月"
                            else -> "日"
                        }
                        state = state.copy(selectedFrequency = fallback)
                    }
                    if (sel == "周" && !result.weekly.available) {
                        state = state.copy(selectedFrequency = "日")
                    }
                    if (sel == "月" && !result.monthly.available) {
                        state = state.copy(selectedFrequency = "日")
                    }
                    state
                }
            },
            onFailure = {
                _uiState.update { s -> s.copy(isCheckingFrequencies = false) }
            }
        )
    }
}
```

- [ ] **Step 3: 构建验证**

```bash
cd machine_check
./gradlew :app:assembleDebug
```

预期：BUILD SUCCESSFUL。

- [ ] **Step 4: Commit**

```bash
git add app/src/main/java/com/machine_check/inspection/ui/scan/ScanViewModel.kt
git commit -m "perf: add 300ms debounce + Job cancel to device model input

- Cancel previous freqCheckJob before launching new one
- Add 300ms delay debounce to avoid API call per keystroke
- Prevents coroutine pileup and stale result overwrites

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 9: 手机端 — QR 扫码器资源释放

**Files:**
- Modify: `machine_check/app/src/main/java/com/machine_check/inspection/ui/components/QrCodeScanner.kt`

- [ ] **Step 1: 将 barcodeScanner 和 analysisExecutor 提升到 remember 作用域，并在 DisposableEffect 中释放**

修改 `QrCodeScanner.kt` 中的 `AndroidView` factory lambda（约第108-173行）。核心改动：将资源变量移到 lambda 外以便在 `DisposableEffect` 中访问。

将第128-129行：
```kotlin
val barcodeScanner = BarcodeScanning.getClient()
val analysisExecutor = Executors.newSingleThreadExecutor()
```

改为在 `AndroidView` factory 外部定义 `remember` 变量（在 `Box` 之前，约第106行之后）：

```kotlin
// 资源引用（用于 DisposableEffect 清理）
var barcodeScannerRef by remember { mutableStateOf<com.google.mlkit.vision.barcode.BarcodeScanner?>(null) }
var analysisExecutorRef by remember { mutableStateOf<java.util.concurrent.ExecutorService?>(null) }
```

并在 factory 内部分配后保存引用：
```kotlin
val barcodeScanner = BarcodeScanning.getClient()
barcodeScannerRef = barcodeScanner
val analysisExecutor = Executors.newSingleThreadExecutor()
analysisExecutorRef = analysisExecutor
```

需要额外 import：
```kotlin
import com.google.mlkit.vision.barcode.BarcodeScanner
```

- [ ] **Step 2: 更新 DisposableEffect 添加清理逻辑**

将第188-192行的 `DisposableEffect` 改为：

```kotlin
// 组件销毁时清理资源
DisposableEffect(Unit) {
    onDispose {
        lastScannedCode = null
        analysisExecutorRef?.shutdown()
        barcodeScannerRef?.close()
    }
}
```

- [ ] **Step 3: 降低 barcode 扫描分辨率（性能优化）**

将第124行的：
```kotlin
.setTargetResolution(Size(1280, 720))
```
改为：
```kotlin
.setTargetResolution(Size(640, 480))
```
（640x480 对 QR 码扫描完全足够，降低 CPU/GPU 消耗。）

- [ ] **Step 4: lastScannedCode 改为 rememberSaveable**

将第63行的：
```kotlin
var lastScannedCode by remember { mutableStateOf<String?>(null) }
```
改为：
```kotlin
var lastScannedCode by remember { mutableStateOf<String?>(null) }
```
实际上 `remember` 对 `lastScannedCode` 用于防止重复扫描是合适的——屏幕旋转后重置是期望行为（避免扫码后旋转屏幕时重复触发）。此项**不做修改**，仅在 commit message 中备注已评估。

- [ ] **Step 5: 构建验证**

```bash
cd machine_check
./gradlew :app:assembleDebug
```

预期：BUILD SUCCESSFUL。

- [ ] **Step 6: Commit**

```bash
git add app/src/main/java/com/machine_check/inspection/ui/components/QrCodeScanner.kt
git commit -m "fix: release QR scanner resources on dispose + lower resolution

- Save barcodeScanner and analysisExecutor refs for cleanup
- Shutdown executor and close ML Kit scanner in DisposableEffect.onDispose
- Reduce target resolution from 1280x720 to 640x480 (sufficient for QR)
- Fixes thread leak on camera rebind and native resource leak

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 10: 手机端 — InspectionRepository 模板缓存

**Files:**
- Modify: `machine_check/app/src/main/java/com/machine_check/inspection/data/repository/InspectionRepository.kt`

- [ ] **Step 1: 添加缓存数据结构**

在 `InspectionRepository` 类内部（约第17行之后）添加内存缓存：

```kotlin
/**
 * 点检数据仓库
 * 封装网络请求，统一处理成功/失败结果
 *
 * @param apiService 可通过构造函数注入以支持测试
 */
class InspectionRepository(
    private val api: ApiService = RetrofitClient.apiService
) {
    // 模板缓存：key = "deviceModel:frequency", value = cached result
    private val templateCache = java.util.concurrent.ConcurrentHashMap<String, CacheEntry<List<InspectionTemplate>>>()

    private data class CacheEntry<T>(
        val data: T,
        val timestamp: Long = System.currentTimeMillis()
    ) {
        fun isExpired(ttlMs: Long = 5 * 60 * 1000): Boolean =  // 5 分钟过期
            System.currentTimeMillis() - timestamp > ttlMs
    }
```

- [ ] **Step 2: 修改 getTemplates 方法使用缓存**

替换 `getTemplates` 方法（第21-42行）为带缓存的版本：

```kotlin
/** 获取指定设备型号的点检模板列表（带内存缓存，5 分钟过期） */
suspend fun getTemplates(deviceModel: String, frequency: String = "日"): Result<List<InspectionTemplate>> {
    val cacheKey = "$deviceModel:$frequency"

    // 命中缓存且未过期
    val cached = templateCache[cacheKey]
    if (cached != null && !cached.isExpired()) {
        return Result.success(cached.data)
    }

    return try {
        val response = api.getTemplates(deviceModel, frequency)
        if (response.isSuccessful) {
            val body = response.body()
            if (body != null) {
                templateCache[cacheKey] = CacheEntry(body)
                Result.success(body)
            } else {
                Result.failure(
                    Exception("获取模板失败: 服务端返回了空响应体")
                )
            }
        } else {
            Result.failure(
                Exception("获取模板失败: ${response.code()} ${response.message()}")
            )
        }
    } catch (e: Exception) {
        // 网络失败时尝试返回过期缓存
        if (cached != null) {
            Result.success(cached.data)
        } else {
            Result.failure(Exception("网络连接失败，请检查网络设置", e))
        }
    }
}
```

- [ ] **Step 3: 构建验证**

```bash
cd machine_check
./gradlew :app:assembleDebug
```

预期：BUILD SUCCESSFUL。

- [ ] **Step 4: Commit**

```bash
git add app/src/main/java/com/machine_check/inspection/data/repository/InspectionRepository.kt
git commit -m "perf: add in-memory template cache with 5-min TTL

- Cache inspection templates in ConcurrentHashMap
- 5-minute TTL, auto-refetch on expiry
- Fall back to stale cache on network failure
- Eliminates redundant API calls when navigating back to inspection form

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 11: 手机端 — 代码质量 (LazyColumn key + Retrofit 日志 + ViewModel DI)

**Files:**
- Modify: `machine_check/app/src/main/java/com/machine_check/inspection/ui/inspection/InspectionScreen.kt:163`
- Modify: `machine_check/app/src/main/java/com/machine_check/inspection/data/network/RetrofitClient.kt:21`
- Modify: `machine_check/app/src/main/java/com/machine_check/inspection/ui/inspection/InspectionViewModel.kt:54`
- Modify: `machine_check/app/src/main/java/com/machine_check/inspection/ui/scan/ScanViewModel.kt:45-48`

- [ ] **Step 1: LazyColumn key 改用 template.id**

在 `InspectionScreen.kt` 第163行，将：
```kotlin
key = { index, _ -> index }
```
改为：
```kotlin
key = { _, item -> item.template.id }
```

- [ ] **Step 2: RetrofitClient 日志条件化**

在 `RetrofitClient.kt` 第20-22行，将：
```kotlin
private val loggingInterceptor = HttpLoggingInterceptor().apply {
    level = HttpLoggingInterceptor.Level.BODY
}
```
改为：
```kotlin
private val loggingInterceptor = HttpLoggingInterceptor().apply {
    level = HttpLoggingInterceptor.Level.BODY
}
```

需要添加 import：
```kotlin
import com.machine_check.inspection.BuildConfig
```

改为：
```kotlin
private val loggingInterceptor = HttpLoggingInterceptor().apply {
    level = if (BuildConfig.DEBUG) {
        HttpLoggingInterceptor.Level.BODY
    } else {
        HttpLoggingInterceptor.Level.NONE
    }
}
```

- [ ] **Step 3: InspectionViewModel 构造函数注入 Repository**

In `InspectionViewModel.kt`, add the repository as a constructor parameter with default:

将第52-54行：
```kotlin
class InspectionViewModel : ViewModel() {

    private val repository = InspectionRepository()
```
改为：
```kotlin
class InspectionViewModel(
    private val repository: InspectionRepository = InspectionRepository()
) : ViewModel() {
```

（删除原来的 `private val repository` 字段声明，直接用构造函数参数。）

- [ ] **Step 4: ScanViewModel 构造函数注入依赖**

将 `ScanViewModel` 第45-48行：
```kotlin
class ScanViewModel(application: Application) : AndroidViewModel(application) {

    private val preferencesManager = PreferencesManager(application)
    private val repository = InspectionRepository()
```
改为：
```kotlin
class ScanViewModel(
    application: Application,
    private val preferencesManager: PreferencesManager = PreferencesManager(application),
    private val repository: InspectionRepository = InspectionRepository()
) : AndroidViewModel(application) {
```

- [ ] **Step 5: 构建验证**

```bash
cd machine_check
./gradlew :app:assembleDebug
```

预期：BUILD SUCCESSFUL。

- [ ] **Step 6: Commit**

```bash
git add app/src/main/java/com/machine_check/inspection/ui/inspection/InspectionScreen.kt \
        app/src/main/java/com/machine_check/inspection/ui/inspection/InspectionViewModel.kt \
        app/src/main/java/com/machine_check/inspection/ui/scan/ScanViewModel.kt \
        app/src/main/java/com/machine_check/inspection/data/network/RetrofitClient.kt
git commit -m "refactor: code quality improvements for mobile app

- LazyColumn key: index → template.id (prevents stale state on reorder)
- RetrofitClient: BODY logging only in DEBUG builds (privacy + perf)
- InspectionViewModel/ScanViewModel: constructor-injectable dependencies
  for testability (default params maintain backward compat)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## 验证清单

完成所有任务后，执行以下端到端验证：

- [ ] **后端**: `dotnet build` 编译通过
- [ ] **后端**: `dotnet ef database update` 迁移成功
- [ ] **后端**: `dotnet run` 启动正常
- [ ] **后端 Swagger 冒烟测试**:
  - `GET /templates/{deviceModel}` — 返回模板
  - `GET /devices` — 返回设备列表
  - `GET /records/monthly?deviceModel=X&year=2026&month=6&page=1&pageSize=5` — 分页正确
  - `GET /frequency-summary?year=2026&month=6` — 统计正确
  - `POST /records/save` — 保存成功
  - `GET /operators/daily?deviceModel=X&year=2026&month=6` — 每日操作者正确
  - `GET /signatures/get?deviceModel=X&year=2026&month=6` — 签名正确
  - `POST /signatures/save` — 保存签名成功
  - `GET /users/list` — 分页正确
- [ ] **手机端**: `./gradlew :app:assembleDebug` 构建成功
- [ ] **手机端**: APK 安装到设备/模拟器，扫码+表单+提交流程正常
- [ ] **索引验证**: MySQL 中 `SHOW INDEX` 确认 3 个新索引存在
- [ ] **EF Core SQL 日志**: 开启日志后对比优化前后 SQL 执行次数
